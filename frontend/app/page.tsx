'use client';
import {FormEvent,useState} from 'react';
import {useRouter} from 'next/navigation';
const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:8000';
export default function Login(){const router=useRouter();const [error,setError]=useState('');const [busy,setBusy]=useState(false);
 async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);try{const r=await fetch(`${API}/api/auth/login`,{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});if(!r.ok)throw new Error('Email or password is incorrect.');const d=await r.json();sessionStorage.setItem('csrf',d.csrf_token);router.push('/overview')}catch(e){setError(e instanceof Error?e.message:'Could not sign in.')}finally{setBusy(false)}}
 return <main className="login"><section className="login-copy"><div className="mark">G</div><p className="kicker">GiftReach AI</p><h1>Outreach with<br/>a careful hand.</h1><p>Research, qualify, and contact corporate gifting prospects—with provenance and restraint built in.</p><div className="craftline"><span>Tanjore</span><span>Wood</span><span>Brass</span><span>Antiques</span></div></section><form className="login-card" onSubmit={submit}><header><h2>Owner access</h2><p>Sign in to open the control room.</p></header><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Password<input name="password" type="password" required autoComplete="current-password"/></label>{error&&<p className="error">{error}</p>}<button disabled={busy}>{busy?'Signing in…':'Sign in'}</button><small>Production access uses secure, HTTP-only sessions.</small></form></main>}
