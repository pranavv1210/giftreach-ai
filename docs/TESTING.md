# Testing

Backend tests exercise authentication, provenance, multiple contacts, policy validation, suppression, idempotency, missing Gmail safety, and emergency stop. They never send real mail. Run `pytest` in `backend`. Frontend verification uses strict TypeScript, ESLint, and a production Next.js build.

